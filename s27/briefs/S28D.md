# S28 LANE D -- THE ADVERSARY AND THE TEST SUITE

You are lane D. Read `s27/S28_CONTRACT.md` first, then `s27/briefs/S28A.md`, `S28B.md`,
`S28C.md` (the three other lanes' plans), then `s26/agentA_FINDINGS.md` (the S26 Adversary's
method and its per-slide qualifier table), `s24/stats_lib.py` docstrings, and `s27/REPORT.md`.

## Your standing duties
1. **Attack every positive within the hour of its ledger entry**, and every finalist whether or
   not it is positive: leakage (grep the lane's script for `nat_ca`, `oracle_rr`, `rr`, `native`,
   `rmsd` and trace every hit; run the NaN-poison test yourself: natives NaN'd, deployable
   outputs bit-identical); tie-breaking by array order; iid vs fold-clustered CI disagreement;
   concentration in the top-10 targets; k_eff for any minimum over K variants or grid; regression
   to the mean on any tuned parameter (nested CV or it is optimism); the control's match to the
   operator's space (same set size, same magnitude, same basis); the second seed and reversed
   fold order for anything positive; power on every null. Verdict per check: STANDS / STANDS
   WITH CAVEAT / VETOED, as a ledger entry `## S28-L<n> -- ADVERSARY CHECK OF S28-L<m>`.
2. **Break whatever looks most promising.** At each hourly status, name the most promising
   number on the board and run the one experiment most likely to kill it: a matched-random
   control the lane did not run, a permuted-channel twin, the built chain if only the point cloud
   was quoted, a different seed, the FAIL18-vs-rest split (a gain carried by 18 targets is a
   different claim), the order-statistic pricing.
3. **Keep the suite green and grow it.** Run `pytest tests/ -q -p no:cacheprovider` (non-AMBER
   files; AMBER files as a separate TEST job under jobrun, one at a time) at the start and after
   every lane commit that touches `tests/` or `s27/`; every new S28 operator gets a test file
   `tests/test_s28_<lane>.py` if the lane did not write one (synthetic data; identity parameters
   reproduce the deployed operator bit-for-bit; NaN-poison). Report counts in the ledger.
4. **Instrument audits when idle**: reproduce one S27 number from its artefact per hour at
   random (state the seed), re-run `python s26/examine.py` once at the end.
5. **The quantum question.** For lanes A and B, insist on the three-way split the brief requires:
   Hamiltonian quality (the objective's optimum), VQE optimisation quality (how close the circuit
   got to it: compare to the exact optimum where it exists, e.g. the eigensolver in B), classical
   ranking performance, and the emitted structure. A gain that survives only when the circuit
   is replaced by its exact classical optimum is not a quantum result; say so.

## Files
`s27/s28_D_FINDINGS.md` (what you checked, vetoed, could not check; the qualifier table for
every number that would reach a report), `s27/RETRACTIONS_S28.md` (append-only; every S28
retraction with the entry it retracts and the artefact), hourly `s27/STATUS.md` lines, your
scripts as `s27/s28_D_*.py`, results `s27/results/s28_D_*.json`. Commit your own files.
Finish each turn with two lines: what stands, what fell.
