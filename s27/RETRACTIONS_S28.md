# RETRACTIONS_S28 (append-only; lane D keeps it)

Every S28 retraction, with the ledger entry it retracts, the entry that retracts it, and the
artefact. Nothing superseded is deleted anywhere; this file is the index.

| id | retracted claim (entry) | retracting entry | artefact | disposition |
|---|---|---|---|---|
| R1 | S28-L7 (lane C): "built-chain contrasts carry a numerical floor near 1e-5 A" (one target) | S28-L18 (lane D) | `s27/results/s28_A_chain_rows.jsonl` vs `s27/results/chain_rows.jsonl :: DIS`, 22 targets: max 0.0186, mean 0.0025 | SCOPE CORRECTION: the floor is 1e-5 to 2e-2 per target (0.003 mean) whenever the two sides of a chain contrast come from clouds that differ at floating-point level; zero when they share a code path. No result changes. |
| R2 | S28-L18 (lane D): the built chain's floor is "0.02 A per target (0.0025 mean)" (22 targets) | S28-L27 (lane D) | `s27/results/s28_A_chain_rows.jsonl :: prod` vs `chain_rows.jsonl :: DIS`, 126 targets: mean -0.0056, 12 above 0.02, 3 above 0.1, 2LNG -0.513 | SCOPE CORRECTION (my own): the tail reaches 0.5 A on one target in 126 and the mean floor is 0.006 A; the reading (branch flips under floating-point input differences; two sides of a chain contrast must share a code path) stands. No result changes. |
| R3 | S28-L35 (lane C2): "the closure claim is FALSIFIED AT THE REGISTERED BAR BY ONE SCORER, CAGEO" (candidate objective for an A2 run) | S28-L36 (lane D) | `s27/results/s28_D_c2_poolmember.json`: CAGEO prefers a random POOL MEMBER to production on 0.618 of targets vs 0.611 for the ORACLE structure (diff -0.007, 0.18x MDE); head-to-head it rates the ORACLE structure worse than 76% of the pool | VETOED: the preference is contraction (production is worse than every pool member under CAGEO on more than half the targets), not nativeness; the closure claim stands; CONTACT is marginal (Type-M) and not a candidate. The registered bar was met; the registered controls lacked a real-geometry, native-blind structure. |
| R4 | S28-L11 (lane D): "the circuit collects a third of its same-sign hopping bound at J = 3"; the same reading in S28-L8b / L21 / L25 (lane B: "34% / 25% of the bound", "sign coherence 0.34 / 0.25") | S28-L41 (lane B, the representability fit) and S28-L43 (lane D) | `s27/results/s28_B_rows.jsonl` REAL J = 3: seed 0 has 43 cells at coherence > 0.5, 0 in (0.1, 0.5], 83 at <= 0.1; seed 1 32 / 0 / 94; `s28_B_represent_rows.jsonl`: 7/24 coherent cells within 0.041 of the fitted F, 17/24 at the J = 0 F | READING CORRECTION (my own and B's): the mean coherence is a MIXTURE of fully coherent and fully incoherent draws (basin selection), not a partial alignment of every state; no number changes (the variance ratios, slopes and departure numbers stand); the sentence form "collects X% of its bound" is retired for "finds the coherent basin on k of n draws". |
