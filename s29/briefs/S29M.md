# S29 LANE M -- THE DATA-PATH MAP, THE CONVENIENCE-CHOICE LIST, AND THE HARNESS AUDIT

You are lane M. Read `s29/S29_CONTRACT.md`, `s29/BRIEF.md` sections 1 (items 5 to 7), 9 and
13, `s29/STATE.md`, then `docs/STATE_BRIEF_2026-09-12.md` (all), `ARCHITECTURE.md`,
`s25/QUANTUM.md`, and the code end to end: `core/data.py` (ESM features, the corpus), the
distogram training and inference in `s8/` (find the trainer and the leave-fold-out loader),
`s8/generate_univ` (the pool universe), `s24/d_harness.py` (`Candidates.from_universe`,
`arm_vqe`, `readout_uniform`, `gate_set_equality`), `s25/qcand_lib.py` (`Encoding`),
`core/quantum.py` (`StatevectorCircuit`, `free_energy`, `cvar_exact`, `grad_cvar_paramshift`,
`run_cvar_vqe`), `s12/instrument.py` (`shipped_score`, `coordinate_average`, `project`,
`ca_rmsd`, `FAIL18`, `selfcheck`), `core/project.py` (`lam_path`, the multi-start), `core/amber.py`
(the optional relaxation), `s24/stats_lib.py`, and the evaluation harness that produces
`s27/results/chain_rows.jsonl` (find it).

## Deliverable 1: `s29/DATAPATH.md`
In your own words, the full path from sequence to built chain as a numbered list of stages.
For EACH stage: the exact function and file; the input and output objects with their shapes
and units; what information is CREATED (from where), TRANSFORMED (how), COMPRESSED (by what
factor, e.g. 17 bins of distance, PCA-32 of a 1280-dim ESM embedding, top-128 of 500, zrank of
a real-valued score) or DESTROYED (the coordinate average destroys the members' identities and
their pairwise relations; the projection destroys the cloud's distances; the tail readout
destroys the amplitudes' signs and the probabilities); and the entropy or dimension count
before and after where it can be stated. Mark the stages where the 68% common-mode error
(S23 L9) enters and where it could in principle be observed. This map is what every other lane
reasons from; make it exact and cite line numbers.

## Deliverable 2: `s29/CONVENIENCE_CHOICES.md`
Every modelling choice made for convenience rather than for a stated scientific reason, with
the file and line, what the choice is, what the alternatives were, whether the record ever
tested it (cite the ledger line) and, if not, what a decisive cheap test would be. Candidates
you must examine (add your own): 17 distance bins and their edges; the L1 loss and the 1/(sd +
0.5) pair weight in `shipped_score`; PCA-32; the corpus split and fold definition; K = 500 and
BLOSUM62 as the retrieval key; top-128 truncation before the VQE; E = zrank (the rank ladder,
which makes every target's Hamiltonian the same up to ties, S25 L17); 7 or 9 qubits with 12
padding states at max + 10 sd; RY/CNOT depth 3; alpha 0.18, T 0.5, 80 Adam iterations, two
seeds; the CVaR tail read as a uniform average at m = 75; the medoid frame; ramah 0.3 and the
multi-start count in the projection; the uniform-weight average rather than any weighting;
where the FAIL18 label comes from; any hard-coded constant in `core/` that is not derived.

## Deliverable 3: the evaluation-harness audit (`s29/s29_M_harness_audit.md` and a ledger entry)
The charter says to check the harness itself. Verify, with commands and numbers: that
`ca_rmsd` is the Kabsch CA RMSD (write a 5-line independent implementation and compare on 10
targets); that the native used is the pinned one (`catrace_prior.npz` / the universe's `nat_ca`)
and that no benchmark file is read anywhere in the tuning path (grep and a poison test that
renames `results/benchmark_manifest.json` in a temp copy); that `pinned_folds` are the pinned
folds and that every leave-fold-out model in the deployed path really excludes its fold (find
the loader; check one target's fold against the distogram model it is served by); that the
production anchor 3.2126 reproduces from `s27/results/chain_rows.jsonl :: DIS` and from a fresh
re-projection on 6 targets; that `ST.compare`'s MDE is 2.8016 x SE and its fold CI is a cluster
bootstrap over the 5 folds (read the code); that the 126 targets are the 126 (count, no
duplicates, lengths 9 to 16). Report anything that fails, and say "the harness is sound" only
if every check passes.

## Discipline
No experiments beyond the audit's checks (each is seconds). Commit as you go; findings
`s29/s29_M_FINDINGS.md`; ledger entries `(date, M)`; two lines to finish each turn. Your map is
due before the other lanes commit to a build, so post DATAPATH.md first, even if
CONVENIENCE_CHOICES.md is still growing.
