# S29 LANE D -- THE ADVERSARY (PERMANENT, ROTATING), THE COST-RMSD METER, THE SUITE

You are lane D. Read `s29/S29_CONTRACT.md`, `s29/BRIEF.md` sections 9, 14 and 16,
`s29/STATE.md`, `s27/s28_D_FINDINGS.md` (the S28 Adversary's method and its qualifier table),
`s27/RETRACTIONS_S28.md`, `s24/stats_lib.py`, and the S28 tooling you inherit:
`s27/s28_D_attack.py`, `s27/s28_D_c2_poolmember.py`, `s27/s28_D_c2_chain_null.py`,
`s27/s28_D_reproduce.py`, `tests/test_s28_D.py`.

## Standing duties
1. Attack every positive within the hour of its ledger entry, and every finalist: leakage grep
   and the NaN-poison test run by you; tie-breaking; iid vs fold CI; concentration; k_eff and
   split-half transfer for any grid or minimum; regression to the mean; the control's match to
   the operator's space; second seed; power on nulls; the charter's list in section 16; for
   quantum claims the ten controls of section 11 (classical equivalent, diagonalised, permuted,
   random, matched budget, untrained, simpler ansatz, order statistic, product-state restriction,
   second seed) and the nine questions. Verdict STANDS / STANDS WITH CAVEAT / VETOED as
   `## S29-L<n> -- ADVERSARY CHECK OF S29-L<m>`. The multiplicity count: every check states
   how many endpoint comparisons the sprint has run so far (read `s29/STATE.md`) and prices a
   1x-MDE positive against the max-over-K null.
2. Build the cost-RMSD meter FIRST: `s29/s29_D_cost_audit.py`, one command that takes a cost
   function f (a python callable on a CA cloud plus the target's context, or a name from the
   S27 channel library) and reports, for the 126 targets with fold-clustered CIs: (a) the
   ladder Spearman rho(f, RMSD) over the per-target ladder {circuit optimum, random signed
   combination, production, Gaussian perturbation, ORACLE circuit structure, native} using the
   structures in `s27/results/s28_A_structs/` and `s28_C2_*` rows; (b) the gradient cosine of
   -grad f at the production cloud with the direction to the native, rigid body removed
   (`s27/s28_A2_local.py` has the machinery; finite differences for non-analytic f); (c) the
   native's percentile in the pool under f (`s27/s28_A_objdiag.py`); (d) the preference for the
   ORACLE structure over production with the pool-member control (`s27/s28_C2_recog_audit.py`).
   A test reproduces the S28 values for the shipped cost (rho -0.40, cos -0.034, percentile
   36.9, pref 0.071 / 0.206 at CA level) to the third decimal. Every number is ORACLE and the
   entry says so; the meter never tunes anything. Post it as a ledger entry and tell the
   coordinator; every lane's proposed objective goes through it before an endpoint run.
3. Keep the suite green: `pytest tests/ -q -p no:cacheprovider` on the light files plus every
   `tests/test_s29_*.py` after each lane commit that touches `tests/` or `s29/`; the heavy files
   (pipeline, integration with VERIFY_SLOW=1 inside the command, the two AMBER files) in a quiet
   window the coordinator announces. Write `tests/test_s29_<lane>.py` for any operator a lane
   left untested (identity parameters reproduce the deployed operator; NaN-poison).
4. Reproduce one S28 number from its artefact per two hours at random (state the seed).
5. Rotation: the coordinator will hand the adversary role to another lane for specific checks;
   when that happens you take that lane's build for the duration and attack your own prior
   conclusions when you return.

## Files
`s29/s29_D_FINDINGS.md` (checked, vetoed, could not check; the qualifier table for every number
that would reach the report), `s29/RETRACTIONS_S29.md` (append-only), `s29/s29_D_*.py`,
`s29/results/s29_D_*.json`, hourly status under `## D` in `s29/STATUS.md`. Two lines to finish
each turn: what stands, what fell.
