# S29 STATUS

## Coordinator
- 2026-09-19 23:35 running: sprint opened; governor v2.5 (band 92-94, hard 95.5); cap 6; six lanes spawning (L, M, T, O, D, X).
- 2026-09-20 00:06 running: 8 lanes (L M T O D X P B). Closed: H1 (ORACLE step exactly 0), the QA-import route (no method exists below 40-50 residues; AF2 pLDDT fails at peptide length). Open and gated: B (T's spectra scope finding 7: lam2/lam1 0.138 raw vs 0.465 centered vs 0.634 agreement), P (projection price), F1 (queued to M), X (configuration space). Guards: contract rule 20 (the cosine is gameable) and the 8x distogram memorisation. Cap 8, box filling.
- next: O's chain ladder (sharded) says what any operator class could reach; D's check of T's predictions; then wave 3 on whichever mechanism survives.

## L
- 2026-09-19 23:36 running: brief, contract, charter 4/6/12/18, STATE, S28 report 1/2/8, state-brief 5, S28-L48/L49 and the ham_lib scorer list read; topic 1 (native-free QA) searches starting.
- next: s29/lit/L_1_native_free_qa.md with the equation/assumption/information test per family; ledger S29-L1.
- 2026-09-19 23:54 done: topic 1 posted (S29-L1, `s29/lit/L_1_native_free_qa.md`, 15 papers): NO published QA method is trained or benchmarked below 40-50 residues; AF2's pLDDT has no within-target skill on 588 peptides of 10-40 aa (McDonald 2023) = external confirmation of finding 8; nothing importable; the one actionable item is S8's unfinished free-energy stage (1/24 targets), a probe for D or O.
- next: topic 2 (correlated error in ensembles and the methods that break it) -- directly serves H1 and finding 11.
- 2026-09-20 00:03 done: topic 2 posted (S29-L8): S23 L9's identity IS Krogh-Vedelsby's ambiguity decomposition, so finding 11 is a LAW; the (1-1/M) covar coefficient gives more-members <= ~0.008 A (derived, caveated); every breaking method needs trainable members / a known control mean / samples of the truth / a known bias ratio, and H1's family needs the last of those (S24 L3 says we do not have it).
- next: topic 3 (decision theory of structure point estimates, distogram calibration) -- serves lane T's contraction theorem.
- 2026-09-20 00:03 MISROUTE: a coordinator instruction beginning "Lane D: two things from lane T's theorem 2 (S29-L7)" (amend `s29/s29_D_cost_audit.py` with the implied-shrink field for rule 20; run T's three predictions as an adversary check) arrived in lane L's thread. NOT ACTIONED HERE: it is lane D's file and lane D's adversary role, I run no endpoint experiments, and D is editing that file now. Please re-send to D.
- next (L, unchanged): topic 3, whose literature half (contraction of a Bayes estimator under an over-confident posterior, calibration, proper scoring rules) is the direct counterpart of rule 20 and will cite it.
- 2026-09-20 00:08 done: topic 3 posted (S29-L12). THE BIG ONE: Blau & Michaeli's perception-distortion theorem (CVPR 2018) makes finding 8 a NECESSITY -- for any distortion measure the RMSD-optimal estimator's output distribution must diverge from the real one, steepest at low distortion, so every realism-type scorer (i.e. every scorer in the library) must disprefer the RMSD-optimal answer. The one unclosed version of the question is ranking WITHIN a matched-realism band. Also: the 25.8% contraction is Jensen's inequality; post-hoc calibration is closed by argmax/median invariance; use CRPS not MAE.
- next: topic 4 (quantum: CVaR-VQE, ADAPT, DLAs, barren plateaus, the condition under which a variational state is not classically reproducible).
- 2026-09-20 00:17 done: topics 4 and 5 posted (S29-L13, S29-L14). Q4: our set-equality theorem IS eq (12) of Barkoutsos (CVaR is defined on SORTED samples, for diagonal H) -- cite it, do not present it as a discovery; CVaR's global optimum is the FLAT set {overlap >= alpha}, a new falsifier for our own quantum arms; non-classicality needs (C1) non-commuting terms AND (C2) a non-eigenvector target (Gibbs/QBM) and we have never had both; arXiv:2312.09121 makes 'it trains' evidence FOR classical simulability; the ansatz's DLA is unmeasured and costs minutes. Q5: no published ceiling exists; our GENERATION (ORACLE 1.71-2.31) is inside the published band and SELECTION is the whole 0.9-1.5 A gap; and I nearly re-imported PEP-FOLD's retrieval key, which is closed here twice -- logged as a near-miss.
- next: all five briefed topics are posted; awaiting topic 6 from the coordinator. Standing offer: the DLA measurement and the CVaR-flat-optimum falsifier are the two cheapest items I surfaced, both for other lanes.
- 2026-09-20 00:24 done: TOPIC 6 posted (S29-L16, `s29/lit/L_6_matched_realism.md`) for lane D. (c) answered NO -- nobody has conditioned on a realism statistic before measuring accuracy ordering (the closest paper, PMC2677743, documents the confound and declines the fix), so D's measurement is NOVEL. (a) the design is published and human-validated (PIRM 2018) and its own result is the warning: the realism index correlates 0.83 BETWEEN bands, unreliably WITHIN them. (b) banding on R destroys R's own ordering power by construction, so the result is interpretable either way; but which method wins depends on how the subset is built, so pre-register the band. (d) the target has a closed form: the thin-band limit is the partial correlation rho_SY.R, exactly zero iff a scorer's link to accuracy is fully mediated by realism -- predictable from three correlations BEFORE any endpoint run, and report skill vs band WIDTH as a curve.
- next: awaiting the coordinator's next assignment. Flagged in the entry: expect a small effect, and a null from the CURRENT library would not close the in-band question (ANDIS: those scorers were fitted for the between-band task) -- D should state which claim is being made before running.
- 2026-09-20 00:31 done: TOPIC 7 posted (S29-L19). URGENT FOR D BEFORE IT FIXES THE BAND: the in-band ordering axis IS compactness (+0.909 with the native's z-scored Rg, S14's flip diagnostic) and every realism statistic in the library is compactness-like, so a compactness-loaded band crushes rho_SY.R to zero BY CONSTRUCTION and the null would be SELF-FULFILLING -- measure rho(R, Rg) first and report it beside the result. Otherwise: the record already answers topic 7 twice (S14 0.986 within / 0.600 across vs 0.638 needed, capacity saturated by a LINEAR model; S12's flat learning curve vs a loud leaked control), the literature's pairwise gains are all BETWEEN-band (RMSD 0-12 A), and the free-energy class may not be orthogonal to compactness either -- gate the AMBER spend on a three-target orthogonality diagnostic.
- next: awaiting the coordinator. If D's band is null, the record says the follow-up is the per-target SIGN question (a conditioning signal), not a better in-band ranker.

## M
- 2026-09-19 23:48 running: brief, contract, charter 1/9/13, STATE, state-brief, ARCHITECTURE, s25/QUANTUM.md and the code end to end read (core/data, predict, quantum, project, amber, pipeline; s8/generate; s24/d_harness; s22/qcand_lib (the brief's `s25/qcand_lib.py` does not exist); s12/instrument; s24/stats_lib; s27/run_vqe_chain + run_pool); `s29/DATAPATH.md` posted (13 stages, every function cited by line, counts measured: 183 features, 372,881 MLP params, 6.6k training chains of which 90.5% are the fold-shared fragments, universe 7k-39k windows, DIS anchor 3.2126 reproduced from chain_rows).
- 2026-09-20 00:04 running: all three deliverables POSTED -- DATAPATH.md (13 stages), CONVENIENCE_CHOICES.md (31 entries, 17 UNTESTED), s29_M_harness_audit.md + S29-L9. Audit: nine checks, ALL PASS (job m_harness_audit3, exit 0, 65 s, 0.549 GB); the harness is sound. One declared non-bit-exactness: the s12 distogram cache differs from a fresh recomputation at float32 scale -- score ORDER differs on 2/126, top-75 SET on 0/126, emitted RMSD 0.00e+00 on 126/126. Check 9 (ORACLE diagnostic): the deployed own-fold model is +2.075 nats WORSE than the four models that saw that fold (4.88x MDE, 5/5 folds) -- leave-fold-out is empirically real AND the distogram memorises training peptides by 8x.
- 2026-09-20 00:26 running: F1 pre-registered (PREREG_S29_M_F1.md) with the rule-10 declaration -- S7 already measured the pure-NLL arm at +0.093 A on this instrument and S25 L12 the L2 arm at +0.018 -- so the registered prior is null-to-worse. 12-target PROBE (NOT evidence for the instrument): PROD 3.3816, LOG 3.6161 (+0.2345, 0.35x MDE, NOT MEASURED), L2RISK 3.5632, built chain. Probe gate (+0.30) says GO to 126. Two caught defects, both mine, both fixed before any claim: the permutation control was the IDENTITY for a sum aggregator, and the runner re-emitted finished arms. Addendum 1 filed: the contraction mechanism is WITHDRAWN (S29-L10, S29-L12) and my own probe corroborates it -- LOG contracts LESS (bond 3.106 vs 2.987) and is WORSE, L2RISK contracts MORE (2.868) and is WORSE, so contraction and RMSD do not track across functionals.
- next: the 126-target run (6 arms incl. SWAPCTL, the zero-information re-ordering at LOG's own 23-of-75 exchange rate), then ST.compare on the built chain, the S24-L3 parallel-bias cosine (probe: 0.927 vs a 0.182 random reference), the FAIL18 split, and a ledger entry.

## T
- 2026-09-20 00:30 BRIEF COMPLETE: THEORY.md carries sections 2, 3, Q1, Q2, 1, 4, 5, 7 (6 is inside Q1), ledger S29-L7/L11/L15/L17 and the entry with this commit; 13 registered predictions, each with a named lane and a minutes-long check. Headlines: theorem 2 (marginal objectives are second order and carry no term in the native); Var = r_stable/D^2 (centering does not restore trainability; only a LOCAL MIXER has extensive stable rank); the deployed stage reduces to a target-independent rank-weight profile whose only endpoint channel is m; contraction is an exact variance identity so calibration cannot touch it; and THE SET-EQUALITY THEOREM FAILS under a CVaR over the tail AVERAGE structure -- the one place the classical counterpart genuinely goes away.
- 2026-09-20 00:32 added: tests/test_s29_T.py, 10 pass in 30 s (the variance law against a direct parameter-shift measurement; CVaR exactly flat above the VaR; the RU optimum against the Gibbs limit and a direct evaluation; the three-state set-equality counterexample; the contraction identity; the prefix cut-rank asymmetry; the transverse fields D/n stable rank). One job, s29T_reach, is still queued behind the launch cap; it only adds a per-target free-energy-gap block and no posted number depends on it.
- next: idle and available. Standing offers: price any proposed objective against theorem 2 before it is built; supply the flat-fraction (M5) and fixed-profile (M6) clauses to lane D; derive the configuration-space mixer cell for lane X if it wants it.

## O
- 2026-09-20 00:33 running: S29-L20 posted (rung 6 KILLS H1: ORACLE cos -0.058 below the random-field 0.144 at 2.19x, ORACLE global step EXACTLY t=0, LFO bit-identical to production 126/126 on the chain; FAIL18 cos -0.317, random-18 p 0.0006) and S29-L21 (rung 8 PC1: ORACLE global eta = 0, sign a coin flip 52%, LFO +0.0071 at 0.82x -> lane B's achievable ceiling through this readout is ZERO). Chain ladder now 5 shards + rung 9 (top-128) launched.
- next: rung 9 (best1 / best prefix-m / hull of the top-128, the quantum stage's whole field of view) posts on its own; then the full ladder table (rungs 1-5) as one ledger entry when the 5 shards finish (~00:55).

## D
- 2026-09-19 23:56 done: cost-RMSD meter built and posted (S29-L2; all four S28 anchors reproduce); prereg checks of O (S29-L3) and X (S29-L4); suite gate 378/3/0 on 17 light files (S29-L5).
- next: attack every positive within the hour; O's rung-6 controls and X's D1/P3 controls when their numbers land; heavy test files in the coordinator's quiet window; hourly S28 reproduction.
- 2026-09-20 00:05 done: S29-L6, the meter's first customer -- lane X's pair log-score is +0.200 ladder rho above the shipped cost on the binding S28 ladder (fold CI [+0.100,+0.343], 5/5) and is the first cost in the record that is uninformative rather than anti-informative there; recognition unchanged (native at the 37.8th percentile, 0.25x MDE from the shipped cost's) and its gradient is undefined on 114/126. Reproduction seed 201 (2FBU / DIS+0.5*LEG_compactness) exact, abs diff 0.0.
- next: lanes M, T, O entries as they post; every lane's proposed cost through the meter on request (`--f module:function`).
- 2026-09-20 00:06 done: S29-L10 (the commit 2b89e440 message says S29-L8; the entry took L10 after L and M posted concurrently -- the ledger heading is authoritative) -- contract addendum 20 is mechanical in the meter (every cosine now carries its shrink signature; the shipped cost's descent EXPANDS, bond x1.044, Rg x1.025, 18/126 contract, so its blindness is not a contraction artefact at the gradient level); two defects in my own meter found and fixed with regression tests (an all-NaN cosine axis crashed the renderer; a partially defined cosine was printed as a measurement). tests/test_s29_D.py 9 pass.
- next: O's and T's entries; heavy test files in the quiet window; reproduction seed 202 at ~02:00.

## X
- 2026-09-19 23:45 running: brief, contract (S29+S28), charter, STATE, S13 dossier, S15 dossier 1/4/5, S20 L1-L4, S21 L1-L28, STATE_BRIEF 4-5, core/quantum.py, d_harness, instrument, stats_lib, s28_B_hop, D's meter draft read; encoding (ii) chosen (3-mer fragments of the DIS top-8, recombined; q = 3*ceil(n/3) <= 18); prereg being written.
- next: s29/PREREG_S29_X.md (nine questions, new angle vs S13/S15/S20/S21/S28-L21, falsifiers, prior), then s29_X_config.py + tests, then the 12-target probe under jobrun.
- 2026-09-20 00:02 running: PREREG_S29_X committed (c0535f01); s29_X_config.py + tests/test_s29_X.py green (12 tests: parents rebuild bit-exactly, H_diag recomputed independently on 3 configs, gamma=0 == core.quantum.free_energy to 0.0e+00, mixer grad vs FD 8.6e-09, uniform readout == coordinate_average 0.0e+00, NaN-poison); probe job s29X_probe1 (2P5H) exit 0, 60.1 s, peak RSS 0.298 GB; s29X_probe12 running on all 12.
- next: analyse -> ledger S29-L<n> with D1/D2/P1-P5 verdicts; lane D's meter already ran cost_nll (ladder rho +0.018 vs DIS -0.182, +0.200 at 1.45x MDE 5/5 folds; native pctile 0.378; gradient zero on 99.2% of coordinates).
## B
- 2026-09-20 00:03 running: brief, S29+S28 contracts, charter 3/4/5/11/12, STATE, S28-L8b/L11/L21/L25/L29/L41/L43/L46/L47, `s27/s28_B_hop.py`, `s27/s28_B2_knn.py`, `s29/s29_T_spectra.py` and lane T's 72 spectra rows + 216 grad rows read; lane T's ledger entry is not yet posted (cited by artefact path). Writing `s29/PREREG_S29_B.md`. One correction already: the brief's gate constant 2.954 is the S10-5 ladder figure (`docs/FINDINGS.md` S10-5, `s27/PREREG_S28_A.md` line 9), NOT this instrument's DIS top-75 average (3.0483 at n=126, 3.2529 on the 12 trainability targets, `s27/results/s28_B_rows.jsonl :: rmsd_dis75`); the gate will be PAIRED against each target's own DIS top-75 average and the 2.954 anchor reported beside it with its provenance.
- next: prereg committed, then `s29/s29_B_compat.py` + `tests/test_s29_B.py`, then measurement 1 (consume T's rows, evaluate the bright line) as its own ledger entry, then measurement 2 (the exact ground state's ORACLE diagnostic, 12 targets) under `--agent S29B`.
## P
- 2026-09-20 00:12 running: brief, S29+S28 contracts (incl. addendum 1 rules 20/21), charter 2/9/10/14, STATE, S23 L1-L6 (L6 read in full), S28-L18/L27b/L39/L43, S29-L7, `s12.instrument.project` + `core/project.py` (lam_path, multi-start, ramah 0.3) read; `s29/PREREG_S29_P.md` committed with the S23 L6 distinction cited by line (L6 lines 165-176: s* is read off the NATIVE and is a property of the (pool, native) pair; g is read off the CLOUD's own bonds, and the emitted chain's CA-CA distance is 3.80 by construction, so an input rescale cannot rescale the output); 10 arms + FLOOR; `s29_P_scale.py` + `tests/test_s29_P.py` green (19 tests); job s29P_factors exit 0, 20.0 s, peak RSS 0.319 GB -- all 126 production clouds reproduce `chain_rows.jsonl::DIS`'s rmsd_cloud with difference EXACTLY 0.0 (126/126); realised g mean 1.352 sd 0.318 range 0.998-2.141 (bond 2.9614, Rg 6.2061 -- S23 L1 reproduced to 4 d.p.); s_span 1.102, s_iso 1.077; s29P_probe6 running.
- next: the 6-target 1e-9 gate, then the 126 sharded into 4 concurrent jobs (26 cells/target, ~3.9 s each), then analysis + ledger. Early diagnostic for the coordinator: g is native-free and correlates +0.50 (Spearman) with production's built-chain RMSD -- a difficulty proxy, not yet an operator.
- 2026-09-20 00:14 running: lane D's S29-L4 holes closed in code before any number (scrambled-chimera order-statistic null for D1, PR-matched Dirichlet random-weight control for P3, Rg+virtual bond per arm, PERM spectrum, R3 captured mass); prereg addendum 1 committed with the measured register cap (q=18 costs 32.5 min/arm, q=15 costs 2.1); 14 tests green; s29X_probe12b running (1A13 first VQE arm 258 s, m=2298 of 32768).
- next: analyse -> D1 against the SCRAMBLED null first, then P1-P5 with power and Type-M printed beside any GO; ledger entry numbered from the tail in one process.
- 2026-09-20 00:15 running: s29P_probe6 (6 targets x 26 cells) is SUSPENDED by the governor -- the box is CPU-bound, not RAM-bound (ram 72.6%, cpu 96.5%, 9 registered jobs, 5 of them lane O shards). Lane P's 126-target run is 3276 projections at ~5.5 s each under this contention (~5 CPU-hours), sharded 4 ways as the coordinator asked; it will take the box as it finds it. Flagging for the coordinator: at 6.43 core-equivalents, 9 concurrent single-threaded jobs is past the point where more shards buy wall time.
- next: gate verdict, then the 4 shards.
- 2026-09-20 00:24 running: probe 3/6 (governor suspended it for 9 of the first 13 minutes; box CPU 93-96% with 8 jobs). Gate so far EXACT on 1A13, 1CS9, 2LNG (PROD == `chain_rows.jsonl::DIS` to 0.0). Two things the probe has already exposed, both registered diagnostics doing their job: (a) on 2LNG the FLOOR arm (a 1e-13 RELATIVE perturbation of the cloud, no geometric change) moves the built chain by 0.509 A -- S28-L27b's 2LNG tail reproduced from the input side, and ORACLE-GRID0.95 (4.457) and 1.05 (4.688) are BOTH better than s=1.00 (4.879), so PROD's multi-start branch is not the best branch there; (b) the arm differences on such targets are a sum of a geometric effect and a branch-flip lottery, so FLOOR -- not zero -- is the right reference for a small mean effect. Prereg addendum 3 filed: CTRL-INV (1/g) is native-free hence deployable, so K=5 in the max-over-K null (conservative).
- next: gate verdict on all 6, then 4 concurrent shards (phase `primary` first so the falsifier lands before the ORACLE grid).
- 2026-09-20 00:31 done: PROBE GATE PASSES -- arm PROD reproduces `chain_rows.jsonl::DIS`'s built chain on 6/6 registered targets with max |diff| EXACTLY 0.0 (1A13, 1CS9, 2LNG, 2NB7, 9BFL, 9BAF). Job `s26/jobs_done/s29P_probe6.json` exit 0, wall 1243.9 s (about 9 min of it governor-suspended), peak RSS 0.296 GB, 156 cells, 7.95 s/projection under 8-job contention. 126-target run launched as 4 concurrent shards (contiguous quarters, per-(arm,target) checkpoint, phase `primary` then `oracle`); all four jobruns are alive and queued behind the cap-8 concurrency limit.
- next: primary phase -> `analyse` -> ledger. Two prereg addenda filed since the last line: #3 CTRL-INV is native-free hence deployable (K=5 in the max-over-K null, conservative); #4 every arm's chain is a feasible point of the SHIPPED projection problem, so each row now carries `obj0` and the arm set is a native-free WIDER MULTI-START of `core.project.fit_multi` -- MS-OBJ/MS-MEAN/MS-ORACLE, with the registered prediction that MS-OBJ lowers production's own objective and NOT the RMSD (charter finding 3 at the projection stage).
- 2026-09-20 00:32 blocked (not stalled): all four s29P_run shards are alive but QUEUED -- `s26/jobs` holds 8 registered jobs (5 lane O chainBCD shards, 2 lane D, 1 lane X) against `launch_cap.json` max_concurrent 8, so lane P currently has zero throughput. The jobruns poll every 5 s and will take slots as they free; nothing is lost (per-(arm,target) checkpoint). Note for the coordinator: the box is CPU-bound at 92-96% on 6.43 core-equivalents, so raising the cap would not add throughput -- the binding resource is cores, and lane O holds 5 of the 8 slots. Lane P needs ~3.3 CPU-hours total (3276 projections at ~3.8 s single-streamed), of which the falsifier needs only the `primary` phase (2142 cells).
- next: shards start as O's finish; `analyse` then ledger the moment the primary phase is complete on 126.
- 2026-09-20 00:36 running: shard 2 started, s0/s1/s3 still queued behind the cap. Two mechanism results landed while queued, both from the cached clouds alone (no projection, seconds of CPU), and both bear directly on wave-2 P1: **P9** the averaging distortion is NOT a contraction but a MONOTONE SHAPE DISTORTION IN SEQUENCE SEPARATION -- cloud/native 0.773 at |i-j|=1, crossing 1.00 near |i-j|=8, 1.10 at 13 (ratio of means; median ratio 0.784 -> 1.048; the native-free profile against the posterior median map has the same shape). S23 L1's "22% short in the bond, 6% short in the envelope" are the two ENDS of this curve. It predicts BOND (g=1.29) is harmful and that the best single scalar sits near 1.05-1.08, which is where s_ISO (1.077) and s_SPAN (1.102) already are. **P10** correcting that profile is REFUTED: dividing each cloud's distance map by the leave-fold-out profile and re-embedding by MDS costs +0.578 A on the point cloud (2.83x MDE, fold CI [+0.417,+0.723], 5/5 folds, 27W/99L), and the ORACLE version fitted on other folds' real natives is no better (+0.582). The r==1 control returns the production cloud at EXACTLY 0.0, so that is the correction and not the re-embedding. Closes the population-profile class of distance-space corrections; a fortiori it strengthens this lane's registered null prior for the scalar arms.
- next: the 4 shards; `analyse`; ledger S29-L<n> with ST.fmt for every arm.
- 2026-09-20 00:36 running: PREREG_S29_B.md committed with two addenda (1c345f07, 9d745692, d3ccf6b6); `s29/s29_B_compat.py` + `s29/s29_B_tta.py` + `s29/s29_B_analyse.py` + `tests/test_s29_B.py` (18 pass). Four jobs queued behind the cap (s29B_gs12, s29B_grad12, s29B_tta_flat, s29B_tta_subset; gs probe 9.6 s/target, peak 0.35 GB). TWO DERIVATIONS REGISTERED BEFORE MEASURING, both now confirmed symbolically by the test suite: (a) the SIGN-MIXING LEMMA -- A_c 1 = G 1 = 0, so every eigenvector with lambda != 0 is orthogonal to 1 and has BOTH signs, hence a p-readout averages the two poles back to the pool mean (lane T's S29-L11(d), reached independently); (b) the f term of the tail-then-aggregate objective is flat on EXACTLY the CVaR term's 437 directions, so the raw 85.5% will NOT fall and the quantity gate G5 must read is the OVERLAP (deployed readout moves along 0 of 511 continuous directions; TTA's moves along 74 and the objective sees all 74).
- next: measurement 1 (independent check of S29-L11's law) and measurement 2 (what the centered ground state selects, ORACLE, 12 targets) as two ledger entries; then the flatness + non-prefix counterexample entry; endpoint HELD for lane D's band experiment as instructed. Measurement 2's signed-readout gate is pre-decided by lane O's S29-L21 (PC1 ceiling exactly 0.0000 A) and the entry will say so.
- 2026-09-20 00:38 running: lane T's S29-L15 ladder adopted in prereg addendum 2 before any number (Gamma grid 0/0.5/1/2 x gamma_gap, gate 1 = TV vs Gamma=0 above 0.45 read FIRST, M6 exact CVaR-optimal law as the strongest classical control, M5 flat fraction, cheapest-first ordering); numpy-2 ndarray.ptp removal fixed (coordinator's catch), full-path smoke green 47 arms; s29X_probe12d running.
- next: gate 1 verdict from the small targets, then gate 2 (SA at matched evaluations, the MAIN comparison), then D1 vs the scrambled null and the endpoint; ledger entry after.
